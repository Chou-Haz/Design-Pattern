public class SimpleCommandTest {
	public static void main(String[] args) {
		Ceilingfan fan = new Ceilingfan("living room");
		
		// fan.turn();
		// fan.turn();
		// fan.turn();
		// fan.turn();
		// fan.turnOff();
		// fan.turn();

		// Light light = new Light();
		// Door door = new Door();
		
		// LightOnCommand lightOn = new LightOnCommand(light);
		// LightOffCommand lightOff = new LightOffCommand(light);
		// DoorOnCommand doorOn = new DoorOnCommand(door);

		// RemoteControl remoteControl = new RemoteControl();

		// remoteControl.setCommand(lightOn,0);
		// remoteControl.buttonWasPushed(0);

		// remoteControl.setCommand(lightOff,1);
		// remoteControl.buttonWasPushed(1);

		// remoteControl.setCommand(doorOn,2);
		// remoteControl.buttonWasPushed(2);
	}
}